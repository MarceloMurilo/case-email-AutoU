# Dependências para desenvolvimento local (inclui torch e sentence-transformers)
# Use este arquivo para instalar dependências localmente:
# pip install -r requirements-local.txt

# Dependências base (mesmas do requirements.txt)
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
openai>=1.57.0
pdfplumber==0.10.3
nltk==3.8.1
scikit-learn>=1.4.0

# Dependências adicionais para modo semântico (apenas local - não instaladas no Render)
sentence-transformers>=2.7.0
torch>=2.0.0

